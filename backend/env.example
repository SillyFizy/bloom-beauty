# Django Environment Configuration
DEBUG=False
SECRET_KEY=your-production-secret-key-here
ALLOWED_HOSTS=your-domain.com,www.your-domain.com,localhost

# PostgreSQL Database Configuration
DB_NAME=bloom_beauty
DB_USER=postgres
DB_PASSWORD=832021
DB_HOST=localhost
DB_PORT=5432
DB_SSLMODE=prefer
DB_CONNECT_TIMEOUT=10
DB_CONN_MAX_AGE=300

# RDS Production Configuration (uncomment for AWS RDS)
# RDS_HOSTNAME=your-rds-endpoint.region.rds.amazonaws.com
# RDS_DB_NAME=bloom_beauty
# RDS_USERNAME=joulina_user
# RDS_PASSWORD=your-secure-password
# RDS_PORT=5432

# Application Settings
# ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2,10.0.2.16,192.168.0.189,192.168.68.127

# API Configuration
API_KEY_CUSTOM_HEADER=HTTP_X_API_KEY

# Email Configuration (for production)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password
DEFAULT_FROM_EMAIL=noreply@joulina.com

# Security Settings
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Static and Media Files (for production)
STATIC_ROOT=/var/www/joulina/static/
MEDIA_ROOT=/var/www/joulina/media/
STATIC_URL=/static/
MEDIA_URL=/media/

# Performance Settings
CONN_MAX_AGE=300

# Optional: Redis for caching (if available)
# REDIS_URL=redis://127.0.0.1:6379/1 